;; Magit and git-* packages

(use-package with-editor
             :ensure t
             :pin melpa-stable)

(use-package git-gutter-fridge+
             :ensure t
             :pin melpa-stable)

(use-package magit
             :ensure t
             :pin melpa-stable)

(use-package magithub
             :ensure t
             :pin melpa-stable)
